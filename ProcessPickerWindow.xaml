<Window x:Class="OmniHax.ProcessPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OmniHax"
        mc:Ignorable="d"
        Title="Open Process" Height="480" Width="640"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <CheckBox x:Name="VisibleOnlyCheck"
                      Grid.Column="0"
                      Content="Only processes with visible windows"
                      VerticalAlignment="Center"
                      IsChecked="True"
                      Checked="VisibleOnlyCheck_Changed"
                      Unchecked="VisibleOnlyCheck_Changed" />

            <TextBlock Grid.Column="1" Text="Filter:" VerticalAlignment="Center" Margin="12,0,6,0" />
            <TextBox x:Name="FilterBox" Grid.Column="2" Width="180"
                     VerticalAlignment="Center"
                     TextChanged="FilterBox_TextChanged" />
        </Grid>

        <ListView x:Name="ProcessList"
                  Grid.Row="1"
                  SelectionMode="Single"
                  MouseDoubleClick="ProcessList_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="PID" Width="70" DisplayMemberBinding="{Binding Id}" />
                    <GridViewColumn Header="Name" Width="190" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Window Title" Width="320" DisplayMemberBinding="{Binding Title}" />
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="OK" Width="90" Padding="0,4" IsDefault="True" Click="Ok_Click" />
            <Button Content="Cancel" Width="90" Padding="0,4" Margin="8,0,0,0" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
